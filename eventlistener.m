classdef eventlistener < handle

    methods

        function simulation_started(~, ~)
        end

        function simulation_ended(~, ~)
        end

        function round_started(~, ~)
        end

        function round_ended(~, ~)
        end

        function step(~, ~)
        end

    end

end